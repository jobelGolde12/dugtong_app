# Backend Notes (Deprecated)

This project now connects directly to Turso (LibSQL) from the React Native app.
All server-side API references previously documented here are deprecated.

See `README.md` and `turso/schema.sql` for current architecture details.
